#include <bits/stdc++.h>

using namespace std;

signed main() {
	:q
    wchar_t a = L'0';
    std::wcout << L"Введите символ:\n";
    std::wcin >> a;
    std::wcout << L"Вы ввели символ '" << a << L"'\n";
    return 0;
}
